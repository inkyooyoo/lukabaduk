# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# GTP 엔진 바이너리·데이터 (용량 제한, 로컬에 두고 GTP_ENGINE_PATH_* 로 지정)
engines/