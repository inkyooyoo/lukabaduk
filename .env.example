# 루카바둑 환경 변수 예시
# 복사: .env.example → .env 후 값 채우기 ( .env 는 git 에 올라가지 않음 )

# ---- 소셜 로그인 (카카오/구글) ----
# 카카오: https://developers.kakao.com/ 앱 생성 후 앱 키, Redirect URI: http://localhost:3000/api/auth/kakao/callback
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# 구글: Google Cloud Console OAuth 2.0 클라이언트 ID/시크릿, Redirect URI: http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---- 서버 ----
# PORT=3000
# HOSTNAME=localhost
# NODE_ENV=development

# ---- 인증 (JWT) ----
# 프로덕션에서는 반드시 다른 값으로 변경
# TOKEN_SECRET=lukabaduk-secret-change-in-production

# ---- AI (GNU Go) ----
# Windows에서 비워두면 프로젝트 내 engines/gnugo-3.8/gnugo.exe 사용
# GTP_ENGINE_PATH_GNUGO=
# GTP_ENGINE_ARGS_GNUGO=--mode gtp
